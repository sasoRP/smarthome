rule "Movement Detected"
when
    Item motion_sensor_sala_jantar_motion changed to ON or
	Item motion_sensor_sala_estar_motion changed to ON or
	Item motion_sensor_quarto_pedro_motion changed to ON
then
	if (Motion_Alarm.state == ON)
	{
		sendBroadcastNotification("Movimento detectado!");
	}
end